name: test CD workflow

on:
  push:
    branches:
      - dev

jobs:
  call-deploy-workflow:
    uses: PlexusisSolaris/httpbin-kube-deployment/.github/workflows/deploy.yml@main
    with:
      tag: "latest"
